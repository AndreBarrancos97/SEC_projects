`timescale 1ns / 1ps
`include "xdefs.vh"

module xled (
		input	   reset,
		input 	    clk,
		input 	    sel,
		input  	data_in,
		output reg led_sel
		);

 always @(posedge clk,posedge reset)
   if (reset)
     led_sel <= 1'b0;
   else if(sel)
     led_sel <= data_in;
     

endmodule
